<h1>Users Admin</h1>
<h2><%= link_to "Add user",  new_user_path%></h2>
<ul>
	<% @users.each do |user| %>
		<li>
			<span><%= link_to user.name, edit_user_path(user.id) %></span>
			<span><%= link_to "X", user_path(user.id), :method => :delete  %></span>
		</li>
	<% end %>	
</ul>